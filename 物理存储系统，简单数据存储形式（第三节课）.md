 ### 物理存储
这一部分·是要读者在数据库的物理层面有基本认识，这对后续的学习至关重要，读者不必搞懂所有部分背后的工作机制，让我们开始吧。
这门课会重点关注**基于磁盘导向的系统架构**，数据库将存储数据在非易失性磁盘上。这意味着我们将不能直接操作磁盘里的数据，我们必须将其写入内存中，编辑后写回磁盘，这个过程代价是十分昂贵的，所以后面的章节将讨论许多关于这个部分的优化。
我们可以参考这样的结构：
![[引用的图片/Pasted image 20260911201819.png|315]] 
`课程原ppt`
DRAM以上，包括他本身，都是易失性存储，比如你的设备断电，所有的数据都会丢失。但是他们性能优越，支持随机访问，读写速度极快，但同时也价格昂贵。DRAM以下，我们称之为非易失性存储，当然如果你的硬盘被猫猫狗狗弄坏了，数据还是会丢失。这部分不支持随机访问，我们不能以字节为单位去随机的访问，而只能以**页**为单位，一页可能4kb，8kb等等，这取决你的设备。在这门课中我们统一将非易失性存储称作**磁盘**，DRAM称作**内存**，至于cpu相关，我们暂且不做讨论。

我们需要对不同层级存储访问的时间有基本认识，最高层级相比最低层级，时间上可能差出数亿倍，课程上给出了十分生动的例子：假设Cache Ref访问数据消耗1秒，那Tape Archives就要耗费31.7年，当然实际操作上会避免这种有巨大代价的操作。请注意不同于算法课，我们对这种常数时间要额外敏感,1mm秒和10mm秒都是O(1)，但是区别巨大,这对书记库系统的构建很重要。

随机访问和顺序访问我们在算法上看是默认代价相同的，但实际硬件上顺序访问要比随机访问快上很多，所以当我们需要从磁盘读写时，应尽可能使用顺序访问。这点很重要，请读者记住，后面我们有很多操作都是基于这一理念，即尽量使用顺序访问，来进行实现的。
### 页
数据库会被存储到磁盘中，对操作系统来说这和其他的一些文件没有什么区别。在磁盘中数据会被分成一个一个**页**，我们刚刚提到过，磁盘需要以页为单位读写数据，页就是文件被按固定的长度分成块，页很重要，希望读者能记住。
在内存中我们会有一个叫做**缓冲池**的部分，其中存放从磁盘读取的页的存储单元叫做帧，所以我们可以看到，数据库的核心工作就是把数据从磁盘移动到内存，查询或者其他任务完成后再写回磁盘。
![[引用的图片/Pasted image 20260912153706.png|463]]
`课程原ppt`
首先我们会把目录加载到内存中，这是特殊的页，会告诉系统数据存放的位置（我们暂且假设目录只是一个文件），比如目录中可能存放每个文件的偏移位置。 请注意，我们本章更多是建立宏观的理解，细节方面我们会在往后的章节中一一介绍。
`当笔者写到这里的时候虽然还没完成所有章节的学习，但也让笔者感慨数据库系统涉及知识的繁杂，也让笔者倍感写这篇笔记的正确，果然实践是验证真理的唯一标准。`
### 数据库如何在磁盘中表示数据
不同数据库大多时候都有自己独特的文件格式，同时优秀的数据库都会自己决定数据的位置和处理，而并非交由操作系统，我们暂且先接受这个事实，具体原因我们会在后面讲述 。
`Andy在讲述这一部分的时候看起来异常兴奋，他在很久以前就支持这一观点了`
一张表中的页大小是固定的，有些数据库支持用户给不同的表设定不同的页大小，但同一张表的页总是相同的。页中会有一个独特的位置来记录它的特殊标识，或许是个偏移量，也可以是个逻辑位置，总之这可以让系统快速的找到它。通常一个页大小在4-16kb，这是读和写的最小单位，一方面不会太小让数据存在太多页中，一方面避免了很小修改时要进行太多无用的工作。
### 元数据
数据库中的每个页会有一块很小的特殊区域来存储该页的标识信息，包括索引，id等信息来帮助数据库检索。比如你将数据存储到不同区域时，当用户试图查找B文件夹中某一页，数据库系统先进入A文件夹，通过某种方式检测到元数据进行简单计算得到某些东西，然后得以进入B文件夹，再通过B文件夹的元数据。同时元数据还包含了重建数据的内容，当有足够多的元数据，即便数据崩溃，也可以重建整个数据，虽然这样会让速度变得很慢。

### 简单行存储
我们会介绍将数据存入磁盘的多种方法，我们本章节会介绍相对简单的行存储，我们假定每一元组都可以完全存入单一的页。因为每一个元组大小相同，所以在开头的元数据我们可以存入一个数，来记录这个页有多少元组，这样我们可以很容易的通过偏移量找到每一个元组。如下图，我们删除在一三之间的二，这会带来一个问题，当我们插入四时，该插到哪里，插入一三之间，这需要你扫描整个页，开销太大，插入三后面，这太浪费空间了，把三挪到二，再尾插入，同样修改的东西太多，因为三的物理地址变化了。所以我们要引入槽式结构。
![[引用的图片/Pasted image 20260916092414.png|287]]
`课程原ppt`
下面是个槽式页面示意图：
![[引用的图片/Pasted image 20260916092504.png|276]]
`课程原ppt`
开头还是存入一些必备的元数据，然后是一个槽数组，这是个固定的数组，用来记录每一个元组的偏移位置（这个部分在头部还是尾部取决于具体的数据库系统）。这样的优点在于比如当你试图删除3时，你可以移动4并改变槽数组中记录的偏移量，也可以空出位置等后续数据填补，总之这个过程都允许你在这个页面里操作，这样的好处在于这个页面可以被加载到内存中，避免了向磁盘访问，毕竟内存访问起来比磁盘快上太多了。 
但依然存在问题，读者如果了解过c/cpp，其中struct数据结构中可以存储许多类性的变量，但是这在数据库访问会出现问题，当数据无法完美对齐的时候，比如下图：
![[Pasted image 20260917192414.png|372]]
`课程原ppt`
比较简单的方案是在每个元素后面补0来对齐，这样可以避免访问的越界，但是势必会浪费一些空间。在学术论文上提出了通过调整数据顺序使得数据可以对齐，比如：
![[Pasted image 20260917192642.png|374]]
`课程原ppt`
但是似乎没有哪个数据库使用这种方案。
### 元标识符
元标识符用来标识元组所在位置，大部分数据库系统不会存储该数据，而是在读取的同时动态的生成，SQLlite会存储这个标识符，并且作为主键，当你输入一个关系但是没有提供主键时，这个标识符会自动充当主键。

### 一些数据类型的处理
#### 浮点数
我们讨论一下浮点数，这遵循IEEE 754规则，double和float速度很快但是无法精确的存储数据，如果读者学习过csapp，对这一点一定很熟悉，笔者不过多介绍。
`说不定笔者下一个就写csapp，他的繁杂程度感觉会是cmu15445的数倍` 
我们在SQL基础章节介绍过numeric，这样我们可以主动的设定精确度，这在某些对精度要求极高的领域将会很重要。
#### 空值
数据库一般会在元组头部设定一个数组来记录该元组的元素是不是空值，少数数据库可能会将某些特殊值设定为空值标识，比如32位无符号数最大值这样，这样会节省一些元数据空间。注意，笔者下面提到的方式只是介绍，就是在每个元素前面设定一个标识来指示空值，比如一个比特位，但是这样浪费了巨大的空间。 ( 这很蠢，不要使用 )
#### 超过页上限的数据
如果一个页无法存下整个数据，我们不可能为此修改所有页的大小，这时我们会引入一个溢出页的概念，在上一个页会有一个指针指向下一个存储溢出部分的页。

#### 巨量的数据
海量的数据是不适合全部存在数据库系统中的，部分数据库提供接口使得可以访问数据库外的文件。
### 结语
如果用一句话概括的话，这章介绍的所有知识和手段都是为了尽量减少数据库访问的开销，也就是尽量访问内存而非访问磁盘。本章内容大体上介绍了数据库的知识，下面我们将花费很多时间具体的介绍我们提到的每一个笼统的概念，所以读者如果在这章感觉什么也没有学到，这很正常，我们将在下一章展开的讲解每一个疑点。
